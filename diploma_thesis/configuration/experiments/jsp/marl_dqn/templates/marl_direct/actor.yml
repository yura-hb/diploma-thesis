
state:
  - kind: 'layer_norm'
    parameters:
      normalized_shape: [5, 5]
  - kind: 'flatten'
  - kind: 'linear'
    parameters:
      dim: 1024
      activation: 'relu'
  - kind: 'linear'
    parameters:
      dim: 1024
      activation: 'relu'


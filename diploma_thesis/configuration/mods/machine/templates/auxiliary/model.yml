
graph:
  - kind: 'graph_model'
    parameters:
      hetero_aggregation: 'mean'

      layers:
        - kind: 'sage'
          parameters:
            in_channels: -1
            out_channels: 32

        - kind: 'add_pool'

      output:
        kind: 'node'
        node_key: 'operation'

parameters:
  model:
    parameters:
      policy:
        parameters:
          noise:
            kind: 'factorized'

          action_selector:
            kind: 'sample'
            parameters:
              is_distribution: False
